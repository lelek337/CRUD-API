export enum resText {
  notId = 'Not valid id',
  notUser = 'User is not found',
  dataChanget = 'Data changed successfully',
  pathDefault = 'Error: path default',
  deletUser = 'User deleted successfully'
}